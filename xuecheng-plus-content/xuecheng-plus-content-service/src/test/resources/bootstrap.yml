#微服务配置
spring:
  application:
    name: content-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.31.133:3306/xcplus_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: 123456
    hikari:
      max-lifetime: 60000 #最大存活时间
      idle-timeout: 60000 #空闲连接时间
      connection-timeout: 50000 #连接超时时间
      maximum-pool-size: 60 #连接池最大数
      minimum-idle: 10 #连接池最小数

# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml

